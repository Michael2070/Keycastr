# Keycastr（快捷键提示）

一个面向 Windows 的键盘快捷键视觉反馈工具。当你按下已登记的快捷键时，它会在屏幕上弹出提示气泡，直观地确认你的每一次操作——特别适合直播、录屏、演示、教学等需要“让观众看见按键”的场景。

> 当前版本：**1.3.3-beta**（更名自“快捷键提示”，设置界面中仍以括号标注）

## 功能特性

- **快捷键视觉反馈**：按下组合键后屏幕立即显示提示气泡，支持重复次数（`Ctrl+S ×3`）、自动淡出、拖动定位。
- **我的快捷键**：手动录制任意按键组合，优先级最高。
- **软件预设库**：内置 25 套常用软件快捷键预设，支持搜索、逐个启用/停用、修改提示文字、一键全部启用/禁用/恢复默认。
- **自动模式**：检测当前前台窗口，自动启用对应软件的快捷键预设；需手动开启并同意隐私声明，且不跨会话记忆。
- **持续输出模式**：按下 `Ctrl/Shift/Alt/Win` 立即显示，并实时追加后续按键与点击次数，适合需要看清自己键位操作的用户。
- **外观自定义**：透明度、显示时长、淡出时长、位置（预设位置或按住拖拽自定义）、背景/文字/边框颜色、字号、固定宽高，兼容不同 DPI 缩放与多显示器。
- **系统集成**：开机自启（注册表 + 启动文件夹双保险）、单实例保护、系统托盘菜单（测试 / 设置 / 重启程序 / 模式开关 / 退出）。

## 内置预设（25 套）

| 分类 | 软件 |
| --- | --- |
| 系统 | Windows 系统 |
| 开发 | Visual Studio Code |
| 办公 | Microsoft Excel / Word / PowerPoint |
| 浏览器 | Google Chrome |
| 效率 | Notion |
| 设计 / CAD | AutoCAD / Siemens NX (UG) / SolidWorks / SketchUp |
| 三维 / 建模 | Blender / 3ds Max / Autodesk Maya |
| 图像设计 | Adobe Photoshop / Adobe Illustrator |
| 视频后期 | Adobe Premiere Pro / Adobe After Effects / DaVinci Resolve |
| 直播 | OBS Studio |
| 音乐 / 音频 | Guitar Pro 8 / Studio One 7 / FL Studio / Ableton Live / Audacity |

预设库中的快捷键均可按需修改或关闭，不会强制生效。

## 快速开始

### 直接使用

下载发行版 [Keycastr-1.3.3-beta.exe](dist/Keycastr-1.3.3-beta.exe)（绿色单文件，无需安装 Python，双击即可运行）。设置保存在 `%APPDATA%\ShortcutNotifier\shortcut_settings.json`，升级或移动 exe 不丢失设置。

### 从源码运行

环境要求：Windows 10/11，Python 3.10+（开发环境使用 3.13）。

```bash
pip install customtkinter keyboard pystray pillow
python shortcut_notifier.py
```

### 打包 exe

```bash
pip install pyinstaller
build_exe.bat
```

打包产物输出到 `dist\Keycastr-x.x.x-beta.exe`，可分享给没有 Python 环境的用户。

## 使用说明

程序启动后驻留系统托盘，右键托盘图标可打开菜单：

- **测试提示**：显示固定测试气泡，确认提示功能正常。
- **设置**：打开设置窗口（我的快捷键 / 软件预设 / 外观 / 系统）。
- **重启程序**：重启软件，用于恢复异常状态。
- **自动模式 / 持续输出模式 / 开机自启**：快速开关，勾选状态实时刷新。
- **退出**：退出程序。

### 设置页说明

- **我的快捷键**：录制并管理自定义快捷键。
- **软件预设**：搜索并管理各软件预设；顶部为自动模式开关。
- **外观**：调整气泡样式与位置，可点击“拖动定位”直接把提示框拖到目标位置。
- **系统**：开机自启、自动模式说明、关于信息（软件名 / 开发者 / 开发环境 / AI 模型）。

## 自定义与开发

快捷键预设数据集中在 `preset_library.py`，格式如下：

```python
PRESET_LIBRARY = {
    'vscode': {
        'name': 'Visual Studio Code',
        'category': '开发',
        'description': 'VS Code 常用快捷键',
        'apps': ['code.exe'],            # 前台程序名，支持 * 通配
        'title_keywords': [],            # 窗口标题关键词（可选）
        'shortcuts': {
            'ctrl+shift+p': {'display': 'Ctrl+Shift+P 命令面板'},
            'f5': {'display': 'F5 开始调试'},
        },
    },
}
```

添加新软件时，在 `PRESET_LIBRARY` 中新增条目，并把 id 加入 `PRESET_ORDER` 列表即可；按键名必须是 keyboard 库可识别的键名（如 `ctrl+shift+p`、`page up`、`f5`）。

## 常见问题

### 调出任务管理器后提示失效

Windows 对低权限进程的键盘钩子有限制，调出任务管理器（尤其是以管理员权限运行时）可能导致按键提示暂时失效。程序内置自动恢复机制（真实按键状态校准 + 轮询兜底），关闭任务管理器后通常会在 1 秒内自动恢复；如仍异常，可通过托盘菜单“重启程序”解决。

### 开机自启不生效

程序默认开启开机自启，首次运行会自动写入注册表与启动文件夹。若被安全软件拦截，可在“系统”页关闭后重新开启。

### 管理员权限窗口无法显示按键提示

当当前窗口以管理员权限运行时，出于系统安全限制（UIPI），低权限进程无法监听其按键输入；这是 Windows 的系统行为，非本程序缺陷。

## 版本历史

| 版本 | 说明 |
| --- | --- |
| 1.1.3 | 初版 |
| 1.1.4 | 基础功能修复，支持打包 exe |
| 1.2.0 | 软件预设库、自动模式、拖拽定位、现代界面 |
| 1.2.1 | 修复拖拽与 DPI 坐标问题，新增自定义提示大小 |
| 1.2.2 | 坐标全面物理化、字号缩放修复、窗口焦点与 Alt-Tab 修复 |
| 1.2.3 | 钩子失效自愈、首次运行自动开启自启、自动模式默认关闭并增加声明 |
| 1.3.0 | 监听架构重写、持续输出模式、设置界面性能优化、托盘重启 |
| 1.3.1 | 重启程序修复、任务管理器监听修复（真实按键状态 + 校准 + 轮询兜底） |
| 1.3.2 | 持续输出模式状态自愈、预设字号 DPI 修复、任务管理器说明 |
| 1.3.3 | 更名 Keycastr，迁移至本仓库，发行版 |

## 技术栈

- Python 3.13
- Tkinter / CustomTkinter（界面）
- keyboard（键盘监听）
- pystray / Pillow（托盘与图标）
- PyInstaller（打包）

## 许可证

暂未指定许可证，仅限个人学习与使用。
